# Copy to .env.local (or set in Vercel's env UI) to point the
# player at your deployed Go server. For local dev you can leave
# this unset — the default is http://localhost:8080.

VITE_SERVER_URL=https://spectator-server.fly.dev
